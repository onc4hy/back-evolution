<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin" >

    <FrameLayout
        android:id="@+id/meditation_bg"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_meditation" >
    </FrameLayout>

    <RelativeLayout
        android:id="@+id/ll_med_action"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" >

        <Button
            android:id="@+id/btn_meditation_setting"
            style="@style/NavBarStyle"
            android:layout_width="80dp"
            android:layout_height="60dp"
            android:layout_alignParentLeft="true"
            android:text="@string/meditation_btn_setting" />

        <Button
            android:id="@+id/btn_meditation_do"
            style="@style/NavBarStyle"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_centerInParent="true"
            android:drawableTop="@drawable/ic_drawer"
            android:text="@string/meditation_btn_start" />

        <Button
            android:id="@+id/btn_meditation_check"
            style="@style/NavBarStyle"
            android:layout_width="80dp"
            android:layout_height="60dp"
            android:layout_alignParentRight="true"
            android:text="@string/meditation_btn_check" />
    </RelativeLayout>

</RelativeLayout>